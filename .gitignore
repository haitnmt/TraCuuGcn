# TraCuuGcn - Global .gitignore
# This file contains only essential global ignore rules

# Configuration files with sensitive data
configs/*.ini
!configs/*.ini.example
!configs/README.md

# OpenID config files (contain secrets)
**/openid.ini
!**/openid.ini.example

# macOS system files (essential to ignore)
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes

# Windows system files (essential to ignore)
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk

# Global temporary files (essential to ignore)
*.tmp
*.temp
*~
*.swp
*.swo

# Global environment files (sensitive data)
.env
.env.local
.env.*.local

# Global backup files
*.bak
*.backup
*_backup

# Global logs
*.log
